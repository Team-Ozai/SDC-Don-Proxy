config:
  target: 'http://localhost:4000'
  phases:
    - duration: 60
      arrivalRate: 1
    - duration: 60
      arrivalRate: 10
    - duration: 60
      arrivalRate: 100
    - duration: 60
      arrivalRate: 1000
  processor: './idGenerator.js'
scenarios:
  - flow:
      - function: 'idGenerator'
      - get:
          url: '/{{id}}'
      # - get:
      #     url: "/risksandchallenges/{{id}}"
      # - get:
      #     url: "/environmentalcommitments/{{id}}"
